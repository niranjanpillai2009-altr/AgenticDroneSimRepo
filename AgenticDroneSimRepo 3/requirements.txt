# Core (always needed)
numpy>=1.24.0

# Simulator adapter (only needed to fly in AirSim/CARLA-Air)
airsim>=1.8.1
msgpack-rpc-python>=0.4.1
# NOTE: the 'carla' module comes from the CARLA-Air build, not pip. See docs.

# Gemini planner
google-genai>=1.0.0
python-dotenv>=1.0.0

# Local planners (Llama / Mistral via Ollama)
ollama>=0.4.0
